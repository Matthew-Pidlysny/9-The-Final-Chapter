@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Threads REQUIRED)

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/UNRHTargets.cmake")

# Set variables
set(UNRH_VERSION @PROJECT_VERSION@)
set(UNRH_INSTALL_PREFIX @CMAKE_INSTALL_PREFIX@)

# Check components
check_required_components(UNRH)